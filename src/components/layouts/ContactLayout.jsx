import React from 'react'

const ContactLayout = ({id}) => {
  return (
    <section id={id} className="p-10 text-white  bg-primaryBlack">
      <h1 className="text-4xl">Contact</h1>
    </section>
  )
}

export default ContactLayout